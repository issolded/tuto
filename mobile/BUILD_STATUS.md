# Build status

GitHub write access verified; isolated branch created.
Android CI is the source of truth for compilation and test results.
No local Android SDK is available. No APK or passing tests claimed until CI completes.

This is a preview slice, not the full native migration. Production backend and web files are unchanged.
