# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment & secrets
.env
server/.env

# WhatsApp session
auth_info
server/auth_info

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env.local

# Drawing working files: the .webp panels are the source of truth (they are what
# gets uploaded to Storage); the .png siblings are intermediate exports.
drawings/**/*.png
drawings/**/_source/

# Zips handed over during asset work — not part of the project.
tuto*.zip
tuto drawings/
